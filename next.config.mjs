/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "imgmedia.lbb.in",
      "images.unsplash.com",
      "cdn-icons-png.flaticon.com",
    ],
  },
};

export default nextConfig;
